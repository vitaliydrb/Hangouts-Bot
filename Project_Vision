replace < > (&lt; &gt;)
replace &amp; with &
replace mdash; with — and ndash with –
replace &nbsp; with space
re.findall(r.'<item>(?P<CTF>.*)</item>)
parse xml with ET
in tree find channel
for item in channel
    get title as ctf name
    get url as link
    get startdate as beginning
    check differences betwen day month year
    if difference ~ 3 week from now: 
        call class method print
 

requirements 
python-dateutil
datetime
